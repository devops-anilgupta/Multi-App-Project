apiVersion: v1
kind: Service
metadata:
  name: frontend
  # namespace: base (should be set or use -n when applying)
spec:
  selector:
    app: frontend
  ports:
    - port: 80          # service port exposed inside cluster
      targetPort: 3000  # container port your app listens on
  type: ClusterIP       # default type, no nodePort here
